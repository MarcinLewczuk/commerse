Place product images in this folder.
Each product's image_url should be stored in the database as /images/<filename> (e.g. /images/example.jpg).
Runtime: Express serves /images/* via static middleware.
Recommended formats: .webp for photos, .png for graphics.
